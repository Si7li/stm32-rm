"""stm32fetch -- scrape ST's documentation catalog, download reference-manual
PDFs, and batch-run rmtables over them. A separate package from rmtables; it
never imports rmtables internals it doesn't own and never modifies rmtables.
"""

__version__ = "0.1.0"
